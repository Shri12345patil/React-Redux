import React, { Component } from 'react';
import './style.css';

export class Header extends Component {
  render() {
    return (
      <header>User - Registration (React Redux)</header>
    )
  }
}

export default Header;
